import 'package:flutter/material.dart';

class ShowTicketChecker extends StatefulWidget {
  const ShowTicketChecker({Key? key}) : super(key: key);

  

  @override
  State<ShowTicketChecker> createState() => _ShowTicketCheckerState();
}

class _ShowTicketCheckerState extends State<ShowTicketChecker> {
  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}